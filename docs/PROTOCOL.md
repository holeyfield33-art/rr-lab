# Protocol

Current milestone flow:

1. A0 reproducibility
2. A1 statistical gate (still an open hypothesis until verified by run artifacts)
3. Stop and review evidence before advancing operator complexity

Current Experiment B status:

- B0 universal negative control: PASS for the recorded baseline run.
- Bpos known-divisibility positive control: available as calibration only.
- Borbit modular-order observable: available as a standalone benchmark.
- B1 zeta plus N factor-selective operator: undefined; no mapping from modular
	frequency to `log(p)` is used.
